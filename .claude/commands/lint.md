---
description: Run lint checks (ktlint + detekt)
---
Run code quality checks.

Arguments: `$ARGUMENTS` (optional: `fix`)

If no argument: `./gradlew ktlintCheck detekt`
If `fix`: `./gradlew ktlintFormat`

Report lint issues found.
